<Window x:Class="ApertureOS.GameActionConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Confirm" Height="480" Width="420"
        Icon="Assets/AppIcon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        UseLayoutRounding="True"
        Background="{StaticResource WindowBackgroundBrush}">

    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid Margin="28">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="160" Height="240" Background="{StaticResource SurfaceBrush}"
                        CornerRadius="{StaticResource CardCornerRadius}" Margin="0,0,0,20" ClipToBounds="True">
                    <Image x:Name="CoverImage" Stretch="UniformToFill"/>
                </Border>

                <TextBlock x:Name="MessageText" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap" MaxWidth="360" Margin="0,0,0,24"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="ConfirmButton" Width="140" Height="42" Margin="0,0,12,0"
                            Style="{StaticResource AccentButtonStyle}" FontSize="14"
                            IsDefault="True" Click="ConfirmButton_Click"/>
                    <Button x:Name="CancelButton" Content="Cancel" Width="140" Height="42" FontSize="14"
                            IsCancel="True" Click="CancelButton_Click"/>
                </StackPanel>

                <TextBlock x:Name="HintText" Text="D-Pad/A to confirm • B to cancel" FontSize="11"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,16,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
